/////////////////////////////////////////////
v7.2607
1. 解决ReadString 和 ReadStrigDirect在读取ascii utf8时,长度计算错误的BUG. 这是前几个版本引发的bug
2. AsmCall和AsmCallEx增加模式7
3. 略微优化了下找图 找色系列接口.
4. 优化驱动加载的速度.
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.2606
1. 解决memory系列盾加载有小概率蓝屏的BUG 
2. 解决SendStringIme发送的文字被截断的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2601
1. 对双击接口做优化(LeftDoubleClick等),某些窗口可以双击了
2. 解决EnableRealMouse的3和4，在移动距离过短时,会出错的bug
3. 解决CreateFoobarxxx接口,如果传递父窗口为0,会导致foobar不可见的 bug
4. 机器码算法变更,主要解决某些机器上,机器码会变动的问题
5. 新增加接口SetReadDataMemoryData
6. 解决通过DmGuardLoadCustom加载的驱动,无法通过UnLoadDriver卸载的bug
7. 优化memory6在win11 24h2的兼容性.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2543
1. 优化图色模式gdi2和dx3. 在某些情况下会有更好的截图表现
2. 驱动和LoadAi支持win11 25h2
3. 搜索内存默认使用CE一样的快速搜索. 并新增加16和17两种搜索模式(带Ex的搜索接口使用)
4. 解决对64位进程使用dx.public.memory后,进行内存搜索时,会无法搜到大于7FFFFFFF内存的BUG.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2520
1. 解决文件名太长会导致某些接口出BUG
2. 优化GetScreenDataBmp和GetScreenData在内存不足时候时可能会造成程序崩溃的问题
3. 解决部分精简系统无法加载驱动的BUG
4. 解决如果把插件放在system32目录,会导致释放驱动还有IME释放失败
5. 解决绑定和SetMemoryHwndAsProcessId冲突的BUG
6. 解决f2的代码逻辑BUG. 导致无法用大的外壳程序替代的BUG,另外从win11 24h2必须使用大的外壳程序来替代,无法通过释放重新分配的方式来运行
7. 判断窗口是否挂起优化,更加准确一些
8. 注册接口增加了端口自定义
9. memory6优化
10. 解决当虚拟显示器副屏幕在主屏幕左边或者上面时,前台鼠标的坐标映射错误的BUG
11. 解决综合工具打开加密字库时,如果配置文件的密码和实际字库密码不同时,会无法解密的BUG
12. 对64位下的32位进程的内存搜索范围增大到4G.
13. 解决解绑时,如果目标进程已经消失,可能会造成解绑崩溃的BUG
14. 解决foobar所在的窗口如果突然崩溃消失,会导致foobar无法关闭的bug
15. 解决假最小化窗口的图标有时会变成自身程序图标的问题
16. 解决GetSystemInfo在某些机器上会造成崩溃的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2450
1. 驱动和LoadAi支持win11 26100(24h2)版本
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2449
1. 解决EnumWindowXXXX系列接口,如果按照窗口打开顺序排序时,有些时候顺序会错误的问题.
2. 优化memory6
3. 优化dx.public.fake.window.min可能最小化后无法恢复
4. 解决在部分win11上绑定64位进程失败的问题.
5. 增加接口SetAsmHwndAsProcessId
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2434
1. 解决最新的win11补丁会让部分绑定参数无法绑定的问题
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.2430
1. 增加接口SetSendStringDelay
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2428
1. 尝试解决部分intel集成显卡驱动的BUG,导致的插件注册(Reg等)或者获取机器码接口会导致程序闪退的问题. 注意此改动可能会导致双显卡的机器上,机器码有变动.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2425
1. 解决在win11下,如果绑定参数里有dx.mouse.position.lock.api,那么EnableBind 0会无法生效的BUG.
2. 注册接口的版本附加信息长度限制增加到了32个字符.
3. 绑定测试工具发布v77. 增加了AiFindPic的测试功能
4. 类库生成工具发布了v28. 支持最新的火山.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2424
1. 解决memory2在7.2410之后的版本会蓝屏的问题
2. 解决在WIN11的最新补丁会导致插件闪退的问题
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2422
1. 综合工具的图像编辑工具可以缩放窗口了
2. 增加AiFindPic AiFindPicEx AiFindPicMem AiFindPicMemEx AiEnableFindPicWindow 共5个接口
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2421
1. 解决hm盾在某些系统上可能会引发蓝屏的问题
2. 优化memory6盾. 比memory5更强力
3. 解决搜索内存接口,对于某些内存属性会搜不到的问题
4. GetOsType的win7和vista的返回值做了调换.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2419
1. 解决inject盾的模式2和3,在nothread为0时,会无法注入的BUG.
2. 解决绑定模式11和13会导致目标进程崩溃的BUG.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2416
1. 绑定测试工具发布了v75
2. 新增加绑定参数dx.mouse.raw.input.active和dx.keypad.raw.input.active
3. 优化dx.mouse.raw.input和dx.keypad.raw.input
4. 解决LoadAi在c#下加载会闪退的BUG.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2412
1. 类库工具发布了v27. (主要是更改了线程模型为sta. 因为mta模型下,有些3方COM接口调用会导致闪退. 比如浏览文件夹等类似的接口)
2. 免注册的dll发布了v15. (原因同上)
3. 绑定测试工具发布了v74. (原因同上)
4. 解决memory5盾,在64G及其以上内存的机器上,无法读取内存的BUG.
5. 文档里特别增加了chrome浏览器,在开启硬件加速后,如何进行后台图色的说明.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2410
1. 答题工具去掉了多坐标的单个坐标限制 答题工具发布了v32
2. 解决inject盾在最新的WIN10 19045会蓝屏的BUG
/////////////////////////////////////////////

/////////////////////////////////////////////
v7.2405
1. 解决LoadAi接口在某些系统上无法加载的BUG
2. 驱动功能全面支持win11 22631
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2353
1. FindPicSim优化,防止有些时候会找不到图
2. 增加接口TerminateProcessTree
3. 解决AsmCall 模式6在部分WIN11下无法正常生效的BUG
4. 解决绑定参数dx.public.fake.window.min在对最大化的窗口绑定后,目标窗口会恢复原始大小的问题
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.2336
1. YOLO综合工具标记逻辑优化. 更加的方便
2. YOLO综合工具增加了默认类. 对于多个同类可以不用每次都进行选择.
3. YOLO综合工具增加可以对模型的输入大小进行修改
4. YOLO的dmx格式变更.新老版本不混用. 新的dmx格式必须配合新的插件和新的ai.module来使用.
5. YOLO的onnx格式下不需要再有class文件了.
6. YOLO相关的教学视频重新录制.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2335
1. YOLO综合工具修复了无法在编辑栏输入空格的BUG
2. YOLO综合工具修复了无法停止训练的BUG
3. 解决插件内存加载在使用AiYoloDetectObjects接口时,会有错误弹窗的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2334
1. 增加了Ai模块,提供了对Yolov5的支持
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2319
1. 解决上个版本的memory5盾在某些情况下会导致蓝屏的问题
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2318
1. 新增加保护盾memory6
2. 解决GetDPI在某些系统下不准确的BUG
3. 优化内存读写速率(开启了SetMemoryHwndAsProcessId的情况下)
4. VirtualQueryEx也可以利用memory系列盾进行突破
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2311
1. 解决b2在win11 22621无法加载的BUG
2. 综合工具在配置里增加一些设置保存
3. 解决找图时,如果图片大小和找图区域一样大时,会导致找图失败的BUG
4. 对dx.graphic.opengl截图增加了一个dx.public.graphic.revert参数. 上下反向
5. foobar系列接口支持以指针的方式创建和绘制,具体查看Foobar相关接口里参数有图片路径的说明
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2302
1. 驱动支持win10(19045)和win11(22621)
2. 综合工具增加功能,在定义文字输入框激活时,按下alt会输入最后一次输入的文字
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2248
1. 优化某些模式,在绑定时,有小概率会卡死在绑定函数里的问题.
2. 解决Assemble DisAssemble和GetRemoteApiAddress的COM版本的DLL，在E语言下用类库封装后调用时,对64位地址解析出错的BUG.
3. 新增加接口SetFindPicMultithreadLimit(当开启了多线程找图时,设置最多使用的线程数量)
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2243
1. 插件无更新. 主要解决后台系统被误报的问题
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2242
1. 新增保护盾memory5
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2240
1. 解决某些窗口无法绑定的问题
2. 综合工具在截图后,自动把范围复制到剪贴板. 在保存图片后,自动把图片名保存到剪贴板.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2235
1. 增加display4盾
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2231
1. 解决win11下，后台鼠标参数dx.mouse.position.lock.api无效的BUG.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2213
1. 解决GetProcessInfo在获取目标进程信息时,如果目标进程内存占用大于4G,返回的内存占用错误的BUG.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2211
1. 绑定时,对于所有可能返回0的地方,都加了错误信息.具体看GetLastError
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2209
1. 所有驱动支持win11 21h2(22000)
2. 解决多线程找图和找内存造成的内存泄漏BUG(只存在于公开版本和少部分定制版本)
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.2149
1. 所有驱动支持win10 21h2(19044)
2. AsmCall对于跳转指令，支持长跳转.(大于128字节的跳转),另外解决对寄存器直接跳转会错误的BUG. 比如jmp rbx等
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2147
1. 综合工具解决了在提取多点找色数据时,如果图片正好是透明图,会导致提取数据错误的BUG.另外提取比例增加了75%和100%
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2144
1. 增加了接口SetInputDm
2. 优化dx.public.graphic.speed
3. 解决沙盘下64位进程无法绑定的问题
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2143
1. GetRemoteApiAddress和GetCommandLine在开启SetMemoryHwndAsProcessId后,会返回失败的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2142
1. 解决开了dx.public.km.protect后,opengl截图失效
2. 前台MoveR可以不需要设置鼠标速度了,仅需要关闭鼠标精确度即可.
3. gr盾可以关闭受保护的句柄
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2137
1. dx.graphic.3d.10plus支持dx12
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2136
1. 解决绑定64位进程时,并且模式不是0和2,如果本进程异常崩溃后,再次启动后绑定,可能会造成目标进程崩溃的BUG
2. 增加了接口SetFindPicMultithreadCount
3. 增加了接口FindPicSim FindPicSimEx FindPicSimE和FindPicSimMem FindPicSimMemEx FindPicSimMemE
4. 优化ForceUnBindWindow有可能造成的崩溃
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.2135
1. 继续优化绑定和解绑导致目标程序崩溃的问题.
2. 解决部分窗口如果用了绑定0或者2,如果窗口关闭后,然后再次对新窗口进行绑定,可能会造成崩溃的问题
3. 解决开启SpeedNormalGraphic后,GetColorRgb和GetColor等获取颜色的接口获取的RGB反了
4. phide对于32位的驱动重新添加上了. 有些老系统还是能用上. 但是调用方法还是保持原来的方法
5. 综合工具增加了均色计算功能
6. 解决SetSimMode 1在部分切换器下,在WIN10系统下,如果切换到其它机器,如果再次调用键鼠功能,会造成蓝屏的BUG.
7. 增加了接口GetSystemInfo和AsmSetTimeout
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2134
1. 解决EnableRealMouse 2 步长为1会崩溃的BUG
2. FindColor FindColorBlock FindMulColor GetColorNum 可以支持反色 颜色描述前面加@
3. 当开启了dx.public.graphic.protect后,优化其稳定性
4. 解决综合工具的二值化保存在有多个颜色的情况下,保存的结果错误的BUG
5. 解决上个版本在绑定和解绑时,有小几率造成目标进程崩溃的BUG.
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.2130
1. 解决绑定模式0 2在绑定32位进程时,会造成目标内存泄漏的BUG.(每次泄漏大概10M,多次绑定解绑会造成目标进程内存不足崩溃)
2. 解决所有绑定模式在绑定后会造成目标程序微量内存泄漏的BUG. (参数越多,泄漏越多,影响以往所有版本. 多次绑定解绑会造成目标进程内存不足崩溃)
3. 解决解绑时,有极小概率造成目标进程崩溃的问题. (影响所有版本)
4. 解决Reg接口注册缓慢的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2129
1. 解决上个版本如果开启了EnableShareDict后,会导致FindStrFastXX和FindStrWithFontXX系列接口失效的BUG.
/////////////////////////////////////////////

/////////////////////////////////////////////
v7.2128
1. 解决如果使用了FindStrFastXXX或者FindStrWithFontXXX,或者开启了EnableShareDict时，如果多线程使用不当,有可能会造成字库序号错误的问题.
2. EnableRealMouse增加了模式3和4.
3. 解决绑定时,如果正好又在另一个线程执行了解绑,有小几率导致程序崩溃的问题.
/////////////////////////////////////////////




/////////////////////////////////////////////
v7.2127
1. 解决某些WIN10某个补丁会造成绑定程序崩溃的问题.
2. 增加dx.public.inject.c绑定属性
3. 增加接口DmGuardExtract和DmGuardLoadCustom
4. 增加接口GetWindowThreadId
5. DownCpu接口变更,可以支持以固定FPS方式降低CPU.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2123
1. 驱动支持win10 19043(21h1)
2. 解决上版本在win8.1系统绑定崩溃的BUG
3. 优化某些接口的实现方式,比如读写内存,GetModuleBaseAddr等接口,可以在开启memory系列盾时有更好的防检测能力.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2120
1. 解决上版本th盾在某些系统上无法获取Win32Address的BUG
2. 优化插件兼容性
3. 综合工具增加了 根据二值化颜色来保存图片的功能. 当勾选二值保存后，点击保存会根据当前的二值化来保存图片. 方便制作透明图.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2119
1. 解决定制非COM版本的插件后,无法正常调用FindDoubleXXX,FindFloatXXX等接口的BUG
2. 保护盾增加了th盾(线程操作系列接口)
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2118
1. 解决部分运营商无法用域名注册的问题(Reg和RegNoMac)
2. 解决插件绑定64位的chrome浏览器会造成崩溃的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2116
1. 解决部分运营商下注册失败的BUG.(返回-1)
2. 解决绑定64位进程时，如果有dx.mouse.raw.input或者dx.keypad.raw.input时，可能会导致目标进程崩溃的BUG
3. 解决从7.2039开始,调用了LockInput，可能会无法锁定某些按键的BUG. 比如回车键等
4. 解决绑定64位进程时，如果模式是0或者2,如果当前脚本进程无故崩溃,再次启动绑定后，会导致目标进程崩溃的BUG
5. 优化插件稳定性，当脚本进程内存不足时，尽可能避免程序崩溃
6. 当开启dx.public.graphic.protect时,并且同时指定了dx.mouse.api和dx.mouse.api时，可能只有一个生效的BUG.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2111
1. 解决之前版本注册超时设置的过长的问题。导致如果服务器卡顿，会导致注册假死的问题
2. 解决当开启了memory2盾，调用AsmCall模式6,对64位进程执行时可能会导致桌面消失的BUG
3. RegEx和RegExNoMac可以支持域名的方式了.
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.2107
1. 解决多进程同时绑定64位进程时,如果绑定失败，再次绑定会导致卡死的BUG.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2104
1. 解决卸载驱动可能会造成蓝屏的BUG
2. 解决dx.graphic.3d.10plus在某些窗口无法后台截图的问题
3. 绑定工具发布v67. 增加了实时截图的功能.(此功能用于解决后台图色和前台图色不一致的问题,可以用此实时截图的内容来制作字库等.防止前台制作出的字库等无法在后台使用)
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2053
1. 驱动功能支持19042(20H2)
2. 解决inject盾在1903之后的系统上，如果参数是内存加载，可能会导致蓝屏的BUG
3. 解决memory3盾在1903之后的系统上开启蓝屏的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2048
1. 解决上个版本在某些WIN7下绑定会造成进程崩溃的BUG.(同时对上个版本所有的定制重置,编译后为最新版本)
2. 解决f1盾在WIN10 18362之上的系统会蓝屏的BUG
3. 绑定工具优化，解决了对编译非COM dll加载后可能会崩溃的BUG
4. 解决FindInputMethod CheckInputMethod在WIN10 系统上可能会无效的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2047
1. 解决AsmCall模式6在WI10 1909上失败的BUG
2. 解决EnableFindPicMultithread接口对FindPicEx系列接口会不生效的BUG
3. 如果FreePic中有*.bmp或者*.gif时,会无条件的释放所有
4. 绑定工具增加了对IP的支持
5. 优化cl盾在某些情况下会蓝屏的问题
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2042
1. 解决之前版本注册时,如果某台服务器崩溃,会导致程序卡很长时间的BUG.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2039
1. 解决上个版本，绑定模式0，可能造成目标进程自动解绑的BUG
2. 优化AsmCall模式6的速度
3. 综合工具的取色功能，可以支持回车了
4. 解决在WIN10系统，多次绑定64位程序，可能会造成目标程序崩溃的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2031
1. 解决绑定模式0，在解绑32位的进程时，可能会造成进程崩溃的BUG.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2027
1. 驱动兼容win10 2004(19041)
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2014
1. 解决综合工具中，当开启浮动抓图时，点击刷新时，刷新的永远是上一张图片的BUG
2. 解决综合工具的快捷键截图后，有可能无法用方向键调整鼠标位置的BUG
3. 解决GetCursorShape有极少数情况下会导致程序崩溃的BUG
4. 当绑定参数里有了dx.public.down.cpu时，就让图色方式降低CPU失效。避免两次降低CPU，影响效率
5. 综合工具里常用参数都进行了保存，包括密码等。以免每次都需要输入密码.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.2002
1. 驱动功能兼容win10 18363(1909)
2. 综合工具里把字库路径和多点找色的3个参数都加入ini保存
3. 优化绑定模式0，当绑定程序突然强制结束后，导致目标程序崩溃的问题
4. 解决GetProcessInfo当pid为当前进程时,会无法获取内存使用量的BUG.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1949
1. 解决某些系统加载驱动蓝屏的问题
2. 解决1903下，绑定32或者64位时,dx.mouse.input.lock.api无效的，并且会导致目标程序崩溃的BUG
3. 解决当绑定雷电模拟器内部的64位进程时，会导致外部鼠标无法操作内部的BUG.
4. 综合工具里，随机多点找色生成时，可以指定偏色.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1941
1. 解决FoobarCreateXXX系列接口，在创建时有可能造成崩溃的BUG
2. 解决GetProcessInfo在pid无效时会崩溃的BUG. 
3. GetProcessInfo的内存使用量采用进程专用内存量,和任务管理器默认的一致
4. 解决GetOsType在某些WIN10或获取版本错误
5. 增加接口GetCpuUsage和GetMemoryUsage
6. AsmCall增加了Label功能. 可以支持跳转功能. 具体可以看AsmAdd里的例子
7. 去除EnableFindPicGray. 用偏色来决定使用何种方式找图。
8. 解决GetRemoteApiAddress对于太长的API获取返回0的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1934
1. 解决驱动在部分WIN10加载时有小几率蓝屏的问题
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1933
1. 解决上个版本当开启EnableShareDict后,副对象会ocr失效的BUG
2. 解决开启EnableShareDict后,如果调用了FindXXXFast或者FindXXXFont系列接口会导致程序崩溃的BUG
3. 接口IsDisplayDead在目标窗口消失后,立刻返回1,而不是继续在等待.
4. 增加接口EnableFindPicMultithread
5. 增加接口EnableFindPicGray
6. 优化EnableFakeActive，可以解决某些窗口无法组合键的问题.
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.1931
1. 解决开启EnableShareDict后，有小几率引发程序崩溃的问题
2. 解决在后台键盘下,无法模拟shift+按键输出大写字符的问题.
3. 解决AsmCall 6在部分系统下，对64位进程无效的BUG.(7.1924之后)
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1928
1. GetDiskSerial接口变更,增加接口GetDiskModel GetDiskReversion
2. 增加接口IsSurrpotVt
3. foobar的父窗口设置为0的话,可能引起本进程崩溃
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.1926
1. 解决定制版本在卸载驱动后,再次加载会造成蓝屏的问题.(主要是win10 1803之后)
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1925
1. 解决获取机器码接口在没有初始化COM的线程里调用时,会导致机器码变动的BUG
2. 解决上个版本驱动加载后，没有删除驱动的BUG
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.1924
1. 驱动全面支持win10 18362
2. 解决UnLoadDriver在某些系统会引发蓝屏的BUG
3. 更改SpeedNormalGraphic的实现方式,可以在WIN8及其以上系统上实现超快截图
4. 解决后台绑定参数中，如果有dx.keypad.state.api或者dx.mouse.state.api时，在某些系统上会绑定失败的BUG
5. 解决AsmCall模式6,在某些系统上无效的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1919
1. 解决上个版本的定制插件无法在老WIN7上加载驱动的BUG
2. 解决在某些WIN10系统上无法获取硬盘序列号的BUG. 同时会造成机器码有变更
3. 增加接口GetModuleSize
4. 解决GetRealPath在某些网吧系统上无法正确获取路径的问题
5. 解决SelectDirectory和SelectFile在某些时候会造成程序崩溃的BUG
6. 解决SetClientSize在某些窗口上会造成设置大小不正确的BUG
7. 解决综合工具色彩描述里如果有灰度颜色时，结果会错误的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1916
1. 介于很多人要求支持老的WIN7，我把旧的签名想办法整合进去了。 老的WIN7也可以加载驱动了，不会-3了.
2. 保护盾inject接口变更
3. 增加保护盾hl和gr
4. 增加接口DmGuardParams
5. 解决部分WIN10 17763无法加载驱动的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1904
1. 解决EnumWindowXXX系列接口，当Title参数中有个别繁体中文时，会导致无法枚举到窗口的BUG
2. 解决FindXXX系列内存搜索接口，在对64位进程搜索时，无法搜索到高于7FFFFFFFFFFF地址的BUG
3. 解决某些64位窗口，在绑定模式为101 103 11 13时，如果重复绑定，可能会导致失败的问题。 错误码是-16
4. 增加接口AsmSetTimeout

关于最近很多人问的部分win7系统，驱动加载返回-3的问题。这里统一作答.
老签名过期了，新的签名模式是sha256，不再是sha1. 微软也不再颁发sha1签名.
所以，如果要兼容这部分系统，又想用新版本，要么打那个补丁(看文档里的说明),要么那用老版本兼容.
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.1901
1. 所有驱动功能支持win10 17763
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.1830
1. 解决SendStringIme2在某些系统中失效的BUG
2. 解决后台功能在某些WIN10系统下无效的BUG. 比如后台特征码
3. 增加接口ShowTaskBarIcon
4. 解决phide2 phide3无效的BUG
5. 优化后台图色模式dx.graphic.opengl在开启dx.public.graphic.speed的稳定性
6. ExecuteCmd接口变更，增加了超时参数

此版本为稳定版本，推荐更新
/////////////////////////////////////////////

/////////////////////////////////////////////
v7.1825
1. 解决上个版本FindString和FindStringEx无效的BUG
2. 增加接口GetRealPath
3. 解决上个版本有几率造成程序假死的问题，以及CPU有可能暴涨的问题
4. 解决EnumWindow在枚举的窗口非常多时(超过大概500个),返回结果会不全的BUG
5. 恢复对32位系统的SetSimMode 模式1和2，以及32位系统的memory memory2盾的支持
6. 解决AsmCall 6在32位系统下崩溃的BUG
7. 增加保护盾cl
8. 类库工具发布v16
9. 解决UnLoadDriver有小概率蓝屏的BUG

此版本为稳定版本，推荐更新
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1822
1. 解决绑定模式101在解绑时可能会造成目标进程崩溃的问题
2. 解决当同时多很多窗口进行绑定时，可能会导致失败，返回-6，-8的问题
3. 解决上个版本在某些时候会导致卡死的问题
4. 解决上个版本保护盾 hm 0 1无法隐藏EXE的BUG
5. 解决上个版本在某些时候，会导致本进程CPU比较高的问题
6. 增加接口GetOsBuildNumber
7. 增加属性dx.public.hack.speed 和HackSpeed接口
8. 解决模式0和2在解绑时速度有些慢的问题
9. 解决memory4盾在读写某些地址会造成蓝屏的BUG
10. 类库生成工具发布v15, 优化e vc的多线程模板，并增加了c#的多线程模板
11. 增加接口UnLoadDriver

/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1819
1. 插件全面支持win10 17134(1803)版本
2. hm盾解决不能隐藏exe模块的BUG. 隐藏方法为hm 0 1
3. 综合工具增加了快捷键取色，并且取色后自动复制坐标
4. 增加了接口ExecuteCmd来执行CMD命令，并获取返回内容
5. 增加接口SpeedNormalGraphic，用来解决win8 以及WIN10系统前台图色速度太慢的问题
6. 解决d1盾在某些时候无效的BUG,影响的是之前所有版本. 并且当d1盾开启后，可能会引发程序崩溃的BUG
7. 免注册更新10.0  支持最新的WIN10 1803
8. 解决之前几个版本，有小概率造成程序崩溃的BUG.
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.1818
1. AsmCall和AsmCallEx支持64位进程. 同时这两个接口的返回值改为64位整数
2. AsmCode接口变更为Assemble, Assemble接口变更为DisAssemble
3. 增加接口GetRemoteApiAddress,具体查看接口说明
4. 增加绑定属性dx.public.inject.super
5. 优化b2盾蓝屏问题,虽然没彻底解决，但有好转
6. 解决f1盾在WIN10下可以通过某些API获取到路径的BUG
7. FindString系列接口支持查找UTF8字符串
8. 解决LoadPicByte无效的BUG
9. 解决上个版本绑定速度很慢的问题
10. 解决部分系统，绑定可能会错误码-14的问题
11. 解决GetDpi接口在部分系统上会误判的BUG
12. 解决GetWindowProcessPath等接口无法获取64位进程路径的BUG
13. 绑定属性dx.public.memory回归了,还是有很多用户用到
14. 类库生成工具更新,优化vb.net的类
15. 综合工具的多点找色自动生成，可以处理透明色了
16. 优化SetWindowSize和SetClientSize，在部分窗口上可能会模糊的问题
17. 解决后台绑定后，在进行图色操作时，有极小的几率会造成目标进程崩溃，或者图色数据错误的BUG。 影响的版本是6.1720之后所有的版本.
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1814
1. 解决当系统没有c盘时,绑定会失败的BUG
2. 解决win10 f1盾失效的BUG
3. 解决免注册,在WIN10上可能会导致打开选择文件夹对话框崩溃的BUG
4. 解决FetchWord在某些生僻中文上会失效的BUG
5. 解决雷电模拟器上，当强行关闭程序后,下次绑定后图色会失效的BUG
6. 解决101 103模式,如果多次绑定,解绑,会造成目标程序崩溃的BUG
7. 解决FindPic在触发多线程后,如果线程被强行结束,可能会造成程序崩溃的问题
8. 增加接口ReleaseRef SetExitThread
9. 解决DownCpu在dx.graphic.3d.10plus上无效的BUG. 增加接口GetFps
10. 解决64位程序后，解绑再绑定后，图色无效的BUG
11. 增加接口VirtualQueryEx
12. 解决绑定64位程序，在WIN8以及部分WIN10上，返回-25错误的BUG
13. 综合工具里增加了对截取的图片，随机提取多点找色的功能
14. 解决部分窗口，绑定后会消失的问题
15. 解决dx.public.fake.window.min在窗口最小化绑定时，会导致窗口不停的往下跑的BUG
16. 解决dx.public.fake.window.min后，最小化按钮不灵的BUG
17. 略微加速内存读写速度
18. 答题器v31 支持多点返回.
19. 增加接口AsmCallEx 支持指定内存调用
20. 类库生成工具，增加了vc e语言的多线程调用模板，有很大的参考价值,强烈建议使用.
21. 解决AsmCall中seh失效的BUG. 影响的是以往所有版本
22. 优化dx.mouse.focus.input.api，解决有些时候会莫名其妙丢失前台键盘输入焦点的问题
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1807
1. 增加接口SetDisplayRefreshDelay,如果有opengl截图截取到之前图片的用户，可以查看此接口的说明解决.
2. 解决当CPU核数超过32个时，绑定窗口以后，会让当前进程的CPU亲和性限制在前32核的BUG.
3. 定制插件中增加了免注册DLL的编译
4. 解决当d1 add all后，会导致绑定出错的BUG
5. ReadString和WriteString支持UTF8字符串
6. 解决WIN10 16299版本下,dx.mouse.input.lock.api失效的BUG. 导致部分窗口，后台鼠标移动时，前台鼠标也跟着动的问题.
7. 增加接口IsFolderExist
8. 增加接口GetCpuType
9. 解决在部分系统下，绑定64位进程,部分绑定属性可能无效的BUG.
10. sta的dll不再发布了,以后默认都是mta的dll. 如果想要用sta模式的用户，可以在自己的程序中用CoInitialize来控制. 但建议还是用mta模式.
11. 解决win10 10240下,绑定64位进程会失败的BUG
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.1802
1. 解决开了b2盾后,block hm等盾就无法开启的问题
2. 解决灰度文字识别只在工具里生效的BUG
3. d1盾可以拦截所有DLL加载。具体看文档
4. ReadInt和ReadIntAddr增加对无符号整数的读取
5. 解决综合工具用回车键截图范围错误的BUG. 同时综合工具加解密图片，可以直接选择目录(包含子目录)
6. 增加了ReadDataToBin WriteDataFromBin ReadDataAddrToBin WriteDataAddrFromBin4个接口,用于高级用户使用。可以解决ReadData类接口速度太慢的问题
   同时用类库生成工具生成的类，对E语言平台增加了4个读写字节集的接口.
7. 大幅优化内存读写的速度. 大幅优化ReadxxxxAddr和WritexxxxAddr类接口的速度.
8. 增加内存盾memory4
9. 增加display3盾(保护窗口不被截图)
10. 增加SetParam64ToPointer接口，用于解决E语言无法传入长整数参数的BUG.(用新的类库工具生成类后，已经自动调用了此接口，并且所有和长整数相关的接口都封装完毕)
11. 增加接口GetDPI. (方便判断系统的文字缩放是不是100%)
12. 解决内存搜索接口,当开启了多线程时，可能会导致各种异常.(影响的版本是之前所有版本)
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.1753
1. 前台模式的MoveR不再限定屏幕范围
2. 由于目前版本的按键不支持64位自动化变量,增加接口Int64ToInt32给按键使用. 涉及的接口有GetModuleBaseAddr ReadInt ReadIntAddr
3. 增加接口GetLocale和SetLocale,设置当前系统非UNICODE字符集为简体中文
4. 优化dx.public.opengl可能在某些窗口上闪烁太频繁的问题
5. 解决之前版本绑定dx图色后,如果更改了分辨率会导致窗口卡死的BUG
6. 解决EnumWindow在指定了枚举属性32后，可能会导致崩溃的BUG
7. 解决上个版本block盾在WIN7失效的BUG
8. 综合工具增加了灰度颜色滑动条,同时综合工具可以适配分屏
9. 解决了在雷电模拟器上，当解绑以后，再次绑定，可能会造成图色失效的BUG
10. 解决当开启了memory盾后，并且系统上装有QQ，可能造成读写内存蓝屏的问题
11. 解决当指定了绑定属性dx.public.ori.proc,绑定64位进程时会导致进程崩溃的BUG
12. 解决部分64位窗口，鼠标点击失效的问题
13. AsmCall的模式6优化
14. 优化b2盾
15. 恢复phide2 phide3
16. 工具的双击复制只对范围 色彩描述 和多点找色的编辑框生效。 其他编辑框保持默认
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.1750
1. 解决部分WIN10系统驱动蓝屏的问题
2. 解决win10 10240无法加载驱动的问题(插件的驱动功能仅支持windows的正式版本,预览版本由于版本太多,没那精力支持)
3. 解决foobar在某些窗口下会闪烁的问题
4. 解决上个版本，在xp系统下绑定会崩溃的问题
5. 增加接口DisableCloseDisplayAndSleep(xp不支持)
6. 综合工具里，所有的编辑框都可以双击复制了
7. 解决当系统界面改为英文时，会导致插件功能异常的问题
8. 解决当系统安装了某些安全软件，会导致绑定64位进程造成进程崩溃的问题
9. 优化b2盾
10. 优化鼠标硬件模拟下,开启EnableRealMouse可能会导致点击无效的问题
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.1749
1. 所有内存接口支持64位进程内存读写
2. 解决上个版本的驱动在部分系统下蓝屏的BUG
3. 解决上个版本b3盾，导致插件某些函数失效的BUG
4. 优化memory3盾，不会异常了
5. 解决之前几个版本,可能会导致runtime error的BUG
6. SetSimMode 增加模式3
7. 前台图色接口和前台鼠标支持多屏幕(分屏)
8. 优化AsmCall中的汇编语句中的数字，不用必须以0开头了
9. 图色模式dx.graphid.3d不需要必须关闭全屏抗锯齿了
10. 解决dx.graphic.3d在某些情况下无法后台截图的问题
11. 机器码算法稍有变更,主要是GetMachineCode，对于网卡MAC地址的获取，过滤掉了蓝牙网卡和虚拟网卡,以免机器码变动
12. 应用户的要求，32位的驱动功能保留了b2 block phide.
13. 新的版本，必须用类库生成工具7.0之后的版本来生成
14. 增加新的绑定模式11和13,主要用来绑定一些传统方法无法绑定的窗口，比如虚拟机
15. 内存接口变更如下: FindInt FindIntEx IntToData ReadDataAddr ReadDoubleAddr ReadFloatAddr ReadIntAddr ReadStringAddr
    VirtualAllocEx VirtualAllocEx VirtualProtectEx WriteDataAddr WriteDoubleAddr WriteFloatAddr WriteFloatAddr WriteStringAddr
    GetModuleBaseAddr,主要是为了支持64位进程读写. 具体变更查看文档
16. 解决win10 1709下，部分窗口组合键失效的BUG
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.1746
1. 删除绑定参数dx.public.memory
2. 删除了所有32位平台的驱动,并且从此版本开始,不再支持和维护32位平台的驱动.
3. 删除了SetSimMode中的超级模拟1, 之前的硬件模拟2改为1.  ps模拟3改为2
4. 删除了DmGuard和SetSimMode中的驱动隐藏功能
5. 增加了保护盾memory2 memory3 b3 hm inject del
6. 增加了接口EnableShareDict
7. AsmCall增加了模式5和6
/////////////////////////////////////////////




/////////////////////////////////////////////
v7.1743
1. 删除绑定模式1和3. 
2. 解决GetNetTime和GetNetTimeByIp在部分系统下无法正常获取时间的问题
3. 增加接口SetExcludeRegion
4. 所有文字识别接口支持灰度颜色
5. 解决E语言平台，当绑定参数里有dx.graphic.3d时，会导致当前线程无法正常获取时间的问题
6. 更新免注册DmReg.dll，解决如果路径太长会导致无效的BUG
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1740B
1. 解决上个版本，当用模式101绑定了64位进程后，再绑定32位进程，会出现无法绑定的BUG.
/////////////////////////////////////////////



/////////////////////////////////////////////
v7.1740
1. 64位进程的绑定支持101和103模式
2. 解决上个版本，在部分64位虚拟机中绑定，会绑定失败的问题
3. 解决上个版本，如果绑定多个64位窗口，只能第一个绑定成功的问题
4. 增加公共绑定模式dx.public.graphic.speed. (提高dx图色速度,尤其对刷新率很低的窗口很有作用)
5. 类库生成工具发布5.0,解决c++平台生成的类库调用会造成内存泄漏的问题
6. SendStringIme2支持64位进程
7. 解决上个版本,图色dx.graphic.3d在XP系统下绑定失败的BUG
8. 解决部分WIN7系统无法打补丁KB3033929,导致驱动无法加载的问题.
9. 字库上限提升到100个
/////////////////////////////////////////////


/////////////////////////////////////////////
v7.1738
1. 支持对64位进程的绑定。（暂时只支持模式0和2)
2. 删除绑定模式4 5 6 7，合并为0 1 2 3
3. 增加图色模式dx.graphic.dx.10plus,支持对DX10 DX11的后台图色
4. 优化插件，绑定目标窗口时，不要求目标窗口必须以管理员方式启动，也可以正常绑定
5. GetWindowState增加模式9,可以判定目标窗口所在进程是不是64位进程. (方便绑定前的判断)
6. 优化dx鼠标，解决部分窗口的绑定后，如果鼠标参数中存在dx.mouse.position.message时，如果手动移动窗口，窗口会乱晃的BUG
7. 优化dx.graphic.opengl，解决了部分窗口用此模式无法后台图色的问题
8. SendString函数变更，如果窗口句柄为0，则对当前激活的窗口发送文本。另外解决了此函数多次调用会内存泄漏的BUG.

注:此版本改动巨大，请仔细测试后再行更新。 另外，64位进程的绑定,除了dx.public.memory和dx.graphic.3d.8不支持,其他参数都支持.
/////////////////////////////////////////////



/////////////////////////////////////////////
v6.1720
1. 解决WIN10 1703之后的系统上，绑定图色模式为dx2 gdi时，会无法正确截图的问题. (目标程序在绑定后，需要重新打开一次即可，永久生效)
2. win10 1607之后的系统，可能会有无法加载驱动的问题，具体解决办法，查看文档中的64位系统如何加载驱动.
/////////////////////////////////////////////



/////////////////////////////////////////////
v6.1647
1. 解决某些窗口在开启memory盾后，仍然无法绑定的问题. 模式0
2. 由于微软于2017年1月1日停用老的驱动签名，从此版本开始一律换成新的驱动签名.
   但此签名在比较老的64位 WIN7 VISTA系统上是不支持的。 
   解决办法是升级系统，最低要求是win7 sp1.
   另外WIN7 sp1系统上还必须打微软的补丁KB3033929. 
   win8以及以上系统没问题
3. 这个月11月10日定制的一批插件，由于微软签名系统更新，导致这批插件在64位系统无法加载驱动
   这批插件我已经复位，重新给予编译. 请大家留意自己的后台
/////////////////////////////////////////////



/////////////////////////////////////////////
v6.1638
1. 解决最新WIN10 14xxx下，后台鼠标有时会失效的BUG
/////////////////////////////////////////////



/////////////////////////////////////////////
v6.1637
1. 解决最新WIN10 14xxx下，鼠标硬件模拟会导致蓝屏的BUG
2. 解决最新WIN10 14xxx下，后台功能会失效的BUG
/////////////////////////////////////////////



/////////////////////////////////////////////
v6.1550
1. 机器码算法还原到5.1423那个版本
2. 优化解决图色dx.graphic.opengl在某些时候不刷新的问题
3. 解决开启真实鼠标模拟,在曲线模拟时,MoveTo函数会鼠标乱晃的BUG
4. 增加3个接口,SetMouseSpeed  GetMouseSpeed和EnableMouseAccuracy. 具体函数说明请看MoveR和相关接口说明.
   另外,硬件模拟的MoveR函数，如果不配合这3个接口，可能会导致移动不精确。
5. 绑定测试工具的测试脚本功能，脚本行数限制由1W行变为10W行
6. 优化内存读写
/////////////////////////////////////////////



/////////////////////////////////////////////
v6.1545
1. 优化上个版本注册缓慢的问题,同时机器码算法变更
2. 优化上个版本phide盾可能导致程序卡死的问题
3. 优化多线程下读写同时读写内存的速度
4. 增加后台图色模式dx.graphic.opengl.esv2 支持最新的bs模拟器
5. 注册的附加信息长度限制为20
/////////////////////////////////////////////




/////////////////////////////////////////////
v6.1544
1. 插件全面支持WIN10
2. GetOsType可以正确获取到WIN10的版本信息了
3. 机器码算法变更
4. 解决dx.graphic.opengl在窗口不刷新时，可能截图黑屏的问题
5. 解决SendStringIme2 在发送文本过长时，可能出现乱码的BUG
6. 综合工具增加颜色管理,最多可以管理20组颜色
7. 解决上个版本综合工具和测试工具里,按钮被回车触发后，又弹起的BUG
8. 增加接口FaqCaptureString (可以发送文本类型的问题了,同样答题器更新到V30，利用好此功能，可以实现自动答题)
9. 解决当开启EnableRealMouse后,如果移动的坐标在屏幕外,会造成卡死的BUG
10. 增加接口VirtualProtectEx
11. 类库生成工具发布v4.0，同时从此版本开始定制的DLL，如果要生成类库，或者是使用测试绑定工具，必须依赖于读我.txt
12. 读写内存速度优化
13. 沙盘使用插件，方法稍有变更，更加简易了。  具体查看文档里的如何在沙盘里使用插件
/////////////////////////////////////////////



/////////////////////////////////////////////
v6.1540
1. 优化工具检测问题,定制中增加了工具的编译
2. 解决5.1416后，绑定模式1无法绑定的BUG
3. 解决XP下,开启memory盾有可能造成蓝屏的BUG
4. 优化AsmCall模式1，在部分情况下会造成蓝屏或者无效的问题
5. 保护盾增加f2盾
/////////////////////////////////////////////



/////////////////////////////////////////////
v6.1538
1. 64位驱动增加签名
2. 机器码计算稍有变化，主要为了解决虚拟机下机器码重复的问题
3. 优化memory盾
4. 解决ReadData函数，在某些情况下会造成程序崩溃的BUG
5. 优化dx.graphic.opengl模式，在某些情况下，会截图失败的问题，比如vbox架构的android模拟器
/////////////////////////////////////////////


/////////////////////////////////////////////
v5.1423
1. 优化memory盾
2. 解决WaitKey当等待任意按键时，无法等待鼠标按键的BUG
3. ExcludePos等算法系列函数可以支持OcrEx等函数的返回值了
/////////////////////////////////////////////



/////////////////////////////////////////////
v5.1417
1. 优化简单游专用在多开时，可能崩溃的BUG
2. 优化Reg函数速度(均摊服务器)
3. 解决101 103模式在WIN7下，绑定部分窗口可能造成目标程序出错的BUG
4. 增加接口MiddleDown MiddleUp
5. 去除GetNetTimeSafe接口，同时，从服务器删除此函数实现.(服务器压力太大)
6. GetNetTimeByIp支持同时验证多个IP或者域名.
7. GetNetTime内部新增加几组ntp服务器
/////////////////////////////////////////////


/////////////////////////////////////////////
v5.1416
1. 优化上个版本Reg可能让程序崩溃的问题
2. 增加保护盾f1(伪装进程)
3. 从此版本开始，简单游平台专用版本从简单游作者后台下载，外面发布的版本都不能直接应用在简单游平台上.
/////////////////////////////////////////////



/////////////////////////////////////////////
v5.1412
1. 解决上个版本SetSimMode在部分系统下,会导致卡死或者键盘无响应的BUG
2. 优化当绑定类名为#32770的窗口时，用模式1或者101 会无法成功的问题
3. 优化Reg函数速度
4. 综合工具解决某些情况下会自动填写偏色的BUG
5. block盾和memory盾全面支持64位系统
/////////////////////////////////////////////



/////////////////////////////////////////////
v5.1410
1. 解决之前所有版本AsmCall的模式2和模式3 有内存泄漏的BUG. 表现为长时间调用asmcall后，导致目标进程崩溃
2. SetSimMode增加模式5和6 (特别针对ps2的键盘鼠标的硬件模拟)
/////////////////////////////////////////////


